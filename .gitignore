node_modules/
.env
.env.*
.DS_Store
Thumbs.db

# 用户私密配置 — 不要提交（含副本，避免误提交 Go key）
user_config*.json
*.local.json
*.local.ts
*.local.js

# 本地改名/临时配置副本
opencode.json1*

# 压缩包（含 skill 打包，非源码）
*.zip

# 本地临时目录（存放 key 文件等）
.opencode/local/

# 敏感凭证
*.key
*.pem
*.cert
*.p12

# IDE / 编辑器
.vscode/settings.json
.idea/

# SkillHub 上传临时目录
skillhub-upload/

# OS
Desktop.ini
ehthumbs.db


# Agnes local workspace / session artifacts
.agnes/
.opencode/session_summary_*.md

# Benchmark 本地证据资产 — 仅忽略采集到的 case 数据（可能含任务内容/上下文）
# schema(template.yaml) 与说明(README.md) 必须进仓库，不忽略
docs/benchmark/cases/*.yaml
docs/benchmark/cases/*.yml

# 本地出图方案文档（含本地路径/个人工作流，非仓库共享内容）
docs/opencode-moa-出图方案.md
