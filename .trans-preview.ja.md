# OpenCode MoA

> 🌐 言語: 英語 · [中文](README.zh.md) · [日本語](README.ja.md) · [한국어](README.ko.md) · [Español](README.es.md) · [Français](README.fr.md) · [Deutsch](README.de.md)

[![License: MIT](https://img.shields.io/badge/License-MIT-blue.svg)](LICENSE)
[![PRs Welcome](https://img.shields.io/badge/PRs-welcome-brightgreen.svg)](CONTRIBUTING.md)
[![OpenCode](https://img.shields.io/badge/OpenCode-%3E%3D1.3.4-orange.svg)](https://opencode.ai)

> 🔥 **ホット (2026-07):** フラッグシップファーストが **Kimi K3** にアップグレード — 2.8T パラメータ、1M コンテキスト、トップティアのフロンティアモデル。MoA の品質の上限は現在、トップに位置しています。

> **v0.0.15:** 構造化出力、凍結された受け入れ基準、アンチチート、自動ルーティング。詳細は [CHANGELOG](CHANGELOG.md) を参照してください。

> **1つの会話エントリーポイント、22の専門モデルが自動的に協力。シンプルなタスクは Flash（安価）を使用し、複雑なタスクはフラッグシップ（高価）を呼び出します。シンプルなタスクがワークロードを支配し、フラッグシップの呼び出しが最小限に抑えられると、コストは最大約90%削減されます（すべてフラッグシップと比較）。実際の節約はタスクの組み合わせに依存し、コード品質は大幅に向上します。**

<!-- ARCH-IMG -->
![OpenCode MoA Architecture](.github/moa-arch.png)
<!-- /ARCH-IMG -->

OpenCode MoA は OpenCode のためのエージェントの混合構成パッケージです。複数のモデルが **同じ問題について同時に考える** ことを可能にし、単一のモデルでは達成できない出力品質に融合します。ツールを切り替えたり、コードを書いたり、API クォータを持っている必要はありません — ファイルをプロジェクトにドロップし、OpenCode を再起動するだけです。

**22 エージェント · 5 コマンド · 3 スキル · 30 秒でデプロイ**

---

## なぜこれが必要なのか？

デフォルトでは OpenCode は最初から最後まで単一のモデルを使用します。1文字を変更したり、システムアーキテクチャを設計したりするのに同じプロンプト、同じ温度、同じコンテキストを使用します。労働の分業はありません。

**3つの問題:**

1. **コストが制御不能** — シンプルなタスクも高価なモデルを使用し、月々の請求が高止まりする
2. **品質のボトルネック** — 単一のモデルは思考の方法が1つしかなく、盲点に簡単にハマる
3. **フォールトトレランスがない** — モデルがダウンするとフリーズし、フォールバックがない

**MoA の解決策:**

```

You: help me design a message queue solution

    ┌─ flag-arch (Qwen3.7 Max)  ─── 建築家の視点からの計画
    ├─ flag-plan (GLM 5.2    )  ─── PMの視点からの計画
    ├─ flag-eng  (MiniMax M3 )  ─── 実装者の視点からの計画
    └─ flag-fuse (Kimi K3    )  ─── 各々の最良を取り入れ、最適な解決策を提供
```

<!-- COST-IMG -->
![Cost down up to 90%](.github/moa-cost.png)
<!-- /COST-IMG -->

3つの異なるモデルからの3つの独立した計画が自然に「コンセンサス + ダイバージェンス」の構造を形成します。フュージョンモデルはコンセンサスを特定し、それを保持し、ダイバージェンスの部分で最良のものを取り入れます — これは単一のモデルではできないことです。

---

## 前提条件

### 必須

| 要件                | チェックコマンド                  | メモ                                                                                                                                                                                                 |
| ------------------- | ------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| OpenCode がインストールされている  | `opencode --version`           | **>= 1.3.4** （エージェントレベルの `reasoningEffort`/`hidden`/`task` サポート; `openai-compatible` プロバイダーが推論を透過的に渡すため、`forceReasoning` は不要）、[インストール](https://opencode.ai/install) |
| OpenCode Go プラン    | opencode.ai コンソール            | [サブスクライブ](https://opencode.ai/auth)、最初の月は $5、その後は $10/月                                                                                                                                 |
| Git がインストールされている       | `git --version`                | リポジトリをクローンするために使用                                                                                                                                                                                |
| OpenCode Go API キー | opencode.ai コンソールで作成 | Zen コンソール (opencode.ai) で作成                                                                                                                                                              |

### オプション (インストールスクリプトによって必要)

| 要件              | チェックコマンド    | メモ                                                                     |
| ----------------- | ---------------- | ------------------------------------------------------------------------- |
| PowerShell Core    | `pwsh --version` | install.ps1 に必要、Windows にバンドルされているか、`brew install powershell` でインストール |
| jq                | `jq --version`   | JSON マージに必要、`apt install jq` / `brew install jq` でインストール |

> pwsh/jq がなくても大丈夫です — メソッド 1 (AI 自動デプロイ) またはメソッド 3 (手動マージ) を使用できます。

### デスクトップ vs CLI

- **CLI**: すべてのメソッドがサポートされています
- **デスクトップ**: メソッド 1 (AI 自動デプロイ) が最も便利です; メソッド 2/3 は最初にターミナル操作が必要です

> ⚠️ **システムレベルのキー パスは間違って配置しやすい** — 以下の「デプロイ前に読む」で正しいスペルを確認してください。間違ったパスは、デプロイメントが成功したように見えますが、すべてのエージェントが接続できなくなります。

> ⚠️ **デプロイ前に読む: キーパスを誤って配置しないでください**
> プロバイダー + キーを **プロジェクトレベルの `opencode.json`** （デフォルト、自己完結型）または **システムレベル** の共有パスのいずれかに配置してください — **1つだけ** 選択してください。
> システムレベルを使用する場合、正しいパスは次のとおりです:
> 
> - Linux/macOS `~/.config/opencode/opencode.json`
> - Windows `%USERPROFILE%\.config\opencode\opencode.json` （**%APPDATA%\opencode** ではありません）
>   間違ったシステムレベルのパスは「デプロイメントは成功するが、すべてのエージェントが接続できない」結果になります。

---

## 30秒でデプロイ

### メソッド 1: AI 自動デプロイ (推奨)

1. [`docs/opencode-moa.en.md`](https://github.com/ZenHG/opencode-moa/blob/master/docs/opencode-moa.en.md) をダウンロード
2. そのドキュメントを OpenCode にアップロードし、次のように送信:

> このマニュアルからすべての 22 エージェント、5 コマンド、および 3 スキルを現在のプロジェクトにデプロイします

3. AI がすべてのファイルを自動的に作成します。**完了したら OpenCode を再起動してください。**

> いかなるファイルも手動で作成する必要はありません。デプロイメントマニュアル自体がインストーラーです。

### メソッド 2: ワンクリックインストールスクリプト (スクリプトバージョン · CLI フレンドリー)

```bash
# リポジトリをクローン
git clone https://github.com/ZenHG/opencode-moa.git

# プロジェクトディレクトリに入る
cd your-project

# リポジトリから .opencode ディレクトリと .moa 設定をコピー
cp -r ../opencode-moa/.opencode/ .
cp -r ../opencode-moa/.moa/ .

# インストールスクリプトを実行 (自動マージ設定、API キーを保持)
# Windows:
pwsh ../opencode-moa/install.ps1
# Linux/macOS:
bash ../opencode-moa/install.sh
```

> インストールスクリプトは元の `opencode.json` を自動バックアップし、プロバイダーと API キーを保持しながら MoA 設定のみをマージします。
> 
> 注意: このメソッドはリポジトリのバンドルされた `.opencode/` をそのままコピーします — そのエージェントには **中国語の表示名** があります。英語名のエージェントが必要な場合（`@english-name` が可能）、代わりにメソッド 1 を使用してください。

### 任意のモデルをカスタマイズ

MoA は **汎用テンプレート** です — 各エージェントのモデルは変更可能な ID です。各エージェントファイルは次のように始まります:

```yaml
model: opencode-go/<model-id>
```

モデルを交換するには、`.opencode/agents/<agent>.md` のその1行を、アクセス可能な任意の `provider/model-id` に編集します（例: `opencode-go/kimi-k2.7-code`, `opencode-go/glm-5.2`）。再インストールは不要です。自由にミックス＆マッチしてください — テンプレートは何にも縛られません。

### メソッド 3: 手動インストール

```bash
# 1. リポジトリをクローン
git clone https://github.com/ZenHG/opencode-moa.git

# 2. .opencode ディレクトリと .moa 設定をコピー
cp -r opencode-moa/.opencode/ your-project/
cp -r opencode-moa/.moa/ your-project/

# 3. opencode.json を手動でマージ (直接置き換えないでください!)
# opencode.json を開き、MoA の permission.task と agent セクションをマージします
# 既存のプロバイダーとモデル設定を保持します
```

> ⚠️ **`cat >>` を使用して追加しないでください** — JSON フォーマットが壊れます。**直接置き換えないでください** — API キーを失います。
> 
> 注意: このメソッドはリポジトリのバンドルされた `.opencode/` をそのままコピーします — そのエージェントには **中国語の表示名** があります。英語名のエージェントが必要な場合（`@english-name` が可能）、代わりにメソッド 1 を使用してください。

### デプロイメントが成功したかどうかを確認する方法

1. OpenCode を再起動した後、`Tab` を押してエージェントをサイクル（Windows デスクトップクライアント: `Ctrl+.` も機能します）し、「concierge-router」を確認します
2. `@tool-handler` と入力すると応答があります
3. 検証スクリプトを実行: `pwsh .opencode/tests/T0-static-verify.ps1` （デプロイ中に手動ブロック 5.5 によって生成された）、期待される結果はすべて PASS (FAIL=0; システムレベルのキーがある場合、WARN も PASS とカウントされます)

### ワンクリックロールバック

```bash
rm -rf your-project/.opencode/
rm -rf your-project/.moa/
# 手動で opencode.json を復元します（インストールスクリプトが自動バックアップを作成します .bak ファイル）
```

---

## 使い方

**何も学ばずに、ただ話す。** コンシェルジュルーターは自動的にタスクの複雑さを判断し、対応するエージェントチェーンを派遣します。

| あなたの言葉                           | コンシェルジュルーターの動作                                          | 使用されるエージェント               |
| -------------------------------------- | -------------------------------------------------------------------- | ----------------------------------- |
| "この変数の名前を変更して"              | 簡単なタスクとして判断                                               | swift (Flash)                       |
| "ユーザー認証モジュールを書いて"        | ツール層が収集 → 3つの中間層並列 → 融合                             | tool-handler + mid-tier trio + fuse |
| "マイクロサービスアーキテクチャを設計して" | ツール層が収集 → 3つのフラッグシップ並列 → 融合 → 実装 → QA        | full-chain 6 agents                 |
| "このスクリーンショットのUIを復元して"   | 3人のフロントエンド専門家が並列 → リーダーが最良を選択              | frontend quartet                    |
| スクリーンショット付きのメッセージ      | ビジョントランスレーターがテキストに変換 → 通常のルーティング       | vision-translator                   |
| エラーログ/図/複雑なコンテンツ付きのメッセージ | ビジョントランスレーターがコンテンツを分解 → 通常のルーティング     | vision-translator (フォールバック役)  |

**直接 `@` コール:**

```
@swift help me write a hello world
@tool-handler search all TODOs in the project
@flag-arch design a message queue solution
```

**ワンクリックコマンド:**

| コマンド          | シナリオ                                       |
| ----------------- | ---------------------------------------------- |
| `/moa-quick`      | 簡単なタスク、翻訳、設定変更                   |
| `/moa-medium`     | 機能モジュール、バグ修正、単一ファイルのリファクタ |
| `/moa-flagship`   | システムアーキテクチャ、大規模リファクタ        |
| `/moa-frontend`   | UI復元、CSS、スクリーンショット修正             |
| `/moa-describe`   | スクリーンショット/画像をテキストに変換        |

### 自動ルーティング (v0.0.15)

コンシェルジュルーターはキーワード分析に基づいてタスクタイプを自動検出します：

- **探索タスク**: "分析する", "比較する", "理解する", "調査する" → 探索プロンプト + 凍結された受け入れ基準
- **実行タスク**: "修正する", "追加する", "実装する", "デプロイする" → 実行プロンプト + ストップロスルール
- **タスクタイプはパイプライン出力に表示される**: `[Type: Execute]` または `[Type: Explore]`

---

## アーキテクチャ

```
                      concierge-router (Flash)
                                 │
                ┌────────────────┼─────────────────┐
                ▼                ▼                 ▼
             ツール層        意見層            融合層
             Flash + MiMo   3つの並列意見が最良を選択
             (~80% コール)   (~18% コール)        (~2% コール)
```

**ツール層** (Flash + MiMo) — コードを読み、ファイルを検索し、スクリーンショットをテキストに変換します。安価で迅速、自由に呼び出せます。

**意見層** (MiniMax / DeepSeek Pro / Qwen / MiMo-Pro) — 異なる視点からの計画。3つの意見が自然に「合意 + 分岐」の構造を形成します。

**融合層** (Kimi K3 / Qwen-Max / GLM / DeepSeek Pro フォールバック) — 合意を維持し、分岐において最良を選択し、融合に失敗した場合はDeepSeek V4 Proにフォールバックします。フラッグシップの融合は現在 **Kimi K3** (2.8T パラメータ、1M コンテキスト、トップティアのフロンティアモデル) で実行されており、MoAの品質の限界を最前線に押し上げています。

> ⚠️ 以下のコールボリューム比率 (~80% / ~18% / ~2%) は **設計目標** であり、測定された統計ではありません。実際の比率はタスクの複雑さによって異なります。

### 構造化出力

意見と融合エージェントは `---section-name---` マーカーを使用します。意見層: `---memory---` + `---plan---` + `---NOT---`。融合層: メタデータ、合意、計画、NOTリスト、受け入れ基準を含む完全な構造。下流の解析と凍結された受け入れの検証を可能にします。

### 不正防止 (v0.0.15)

実装エージェントが手を抜くのを防ぎます: ベースラインの非回帰、禁止されたアクション (テストのスキップ/モック/削除)、隠れたスポットチェック、実装の差分チェック、ストップロス (アイテムごとに3回のリトライ、回帰時のロールバック)。受け入れ基準テンプレートは `.moa/acceptance-template.json` にあります。

## 22 エージェント

> 英語名は論理的役割であり、中国語は括弧内にある **正確なファイル名** です `.opencode/agents/` の下にあり、`@` で呼び出します (例: `@门童路由员`)。

```
concierge-router (门童路由员, Flash)
 │
 ├── ツール層 ─────────────────────────────────────────────
 │   tool-handler      (工具人, Flash    ) コードを読み、ファイルを検索
 │   tool-handler-mimo (工具人-mimo, MiMo) [hidden]  信頼性のあるファイル読み取り (フォールバック + 並列)
 │   swift             (闪电侠, Flash    ) 簡単なタスクを一発で
 │   vision-translator (视觉翻译官, MiMo ) スクリーンショット/UI→テキスト; ログ/図/文書→分解
 │
 ├── 残差抽出者  (残差提取者,  Flash     ) 計画間の分岐を分析
 ├── 信頼度評価者 (置信度评估者, DS Pro    ) 融合結果の信頼度を評価
 │
 ├── 中間意見層 ─────────────────────────────────────────────
 │   mid-eng      (中级·工程, Kimi K2.6 ) 工学的視点
 │   mid-creative (中级·创意, Qwen3.7 Plus) 創造的視点
 │   mid-coder    (中级·码农, Flash     ) 実践的視点
 │   mid-fuse     (中级·融合, Kimi      ) 3つの計画を融合 [max_tokens: 16384]
 │
 ├── フラッグシップ意見層 ─────────────────────────────────────────────
 │   flag-arch (旗舰·架构, Qwen3.7 Max ) トップレベルのアーキテクチャ
 │   flag-plan (旗舰·规划, GLM 5.2     ) 構造化計画
 │   flag-eng  (旗舰·工程, MiniMax M3  ) 大規模実装
 │   flag-fuse (旗舰·融合, Kimi K3     ) 3つのアーキテクチャ計画を融合 [max_tokens: 16384]
 │   flag-impl (旗舰·实现, Flash) [hidden]  融合された計画に従って実装
 │   flag-qa   (旗舰·质检, DeepSeek Pro) 計画レビュー + コード受け入れ [max_tokens: 16384]
 │
 └── フロントエンド意見層 ─────────────────────────────────────────────
     fe-restore (前端·还原, MiMo       ) ピクセルパーフェクトなUI復元
     fe-logic   (前端·逻辑, Qwen3.7 Plus) コンポーネントアーキテクチャ & 状態管理
     fe-motion  (前端·动效, MiMo-Pro   ) インタラクション & モーション
     fe-lead    (前端·总工, GLM-5.2    ) 3つのフロントエンド計画の中から最良を選択 [max_tokens: 16384]
```

フォールバックエージェント (上記のルーターチェーンには含まれず、融合が失敗したときのみ呼び出されます):

```
fallback (融合·保底, DeepSeek V4 Pro) — 同じ残差強化融合、flag-fuse / mid-fuse / fe-lead が失敗したときに使用

## フォールトトレランス設計

### ツールレイヤーフォールバックチェーン

ツールレイヤーが失敗してもフリーズしない — 自動的にダウングレードする:

```
tool-handler (Flash) failed → immediate retry once
  → retry succeeds → return normally
  → retry fails → tool-handler-mimo (MiMo) failed → immediate retry once
    → retry succeeds → return normally
    → retry fails → ask user:
      A. wait a few minutes and retry
      B. skip tool layer, call opinion layer directly (higher cost)
      C. switch to free model
```

> ほとんどのプロバイダーエラー (502/503/タイムアウト) は一時的なものであり、迅速な再試行は通常成功する。

### フュージョンレイヤーフォールバック

プライマリーフュージョンエージェントが失敗した場合 (STUCK / ERROR_PROVIDER / タイムアウト / 空の結果)、コンシェルジュルーターは自動的に `@融合·保底` (DeepSeek V4 Pro, フォールバック) にフォールバックする:

```
flag-fuse (旗舰·融合, Kimi K3) failed
  → task(@融合·保底) (DeepSeek V4 Pro) → output fallback result
mid-fuse (中级·融合, Kimi) failed
  → task(@融合·保底) (DeepSeek V4 Pro) → output fallback result
fe-lead (前端·总工, GLM-5.2) failed
  → task(@融合·保底) (DeepSeek V4 Pro) → output fallback result
```

フォールバックエージェントは、同じ残差強化フュージョンプロセスを使用する。

### オピニオンレイヤー部分的失敗耐性

個々のオピニオンエージェント (アーキテクチャ/計画/エンジニアリング、フロントエンド復元/ロジック/モーション、中級エンジニアリング/クリエイティブ/コーディング) は、独立して空の結果を返したり、タイムアウトしたりすることがある。システムはこれを優雅に処理する:

```
3 parallel opinion agents dispatched
  → any agent returns empty result → retry that agent once
    → retry succeeds → continue normally
    → retry fails → mark as "degraded" and proceed with N/3 inputs
      → 残差提取者 works with available inputs only
      → 旗舰·融合 applies degraded fusion rules
      → output carries "[Partial] N/3 inputs" label
      → confidence score is adjusted downward
```

劣化したフュージョンルール (N < 3):
- コンセンサスカバレッジの分母は N、ではなく 3
- 欠落した視点は `[Missing: perspective name]` とラベル付けされる
- コンセンサスカバレッジ < 50% は「低信頼度の劣化フュージョン」警告をトリガーする
- 単一ソースフュージョン (N=1) は 0.7 の信頼度ペナルティファクターを適用する

> これは、1つのオピニオンエージェントが失敗したときにパイプラインが停止するのを防ぎます (STUCK) — 一般的なユーザーの不満です。

### 宣言的エージェント前提条件

エージェントのアクティベーションは、ハードコーディングされたルーティングルールではなく、宣言的な `precondition` メタデータによって管理される。各エージェントは、いつアクティブになるべきかを宣言する:

| エージェント | 前提条件 |
|-------|---------------|
| 闪电侠 | いつでも |
| 工具人 | コードベースコンテキストが必要 |
| 视觉翻译官 | プライマリ: `screenshot`; フォールバック: `error_log OR diagram OR long_document OR ambiguous_intent` |
| 中级·工程 | エンジニアリングの複雑さが必要 |
| 中级·创意 | クリエイティブの複雑さが必要 |
| 中级·码农 | 実装の複雑さが必要 |
| 旗舰·架构/规划/工程 | システム設計の複雑さが必要 |
| 前端·还原/逻辑/动效 | フロントエンドタスクが必要 |
| 融合·保底 | フュージョンレイヤーが失敗したとき、またはオピニオンレイヤーが部分的な結果を返したときにアクティブ化される |

条件のアクティベーションはショートサーキットロジックに従う: 前提条件が満たされる → アクティブ化; どれも満たされない → ユーザーに確認を求める。これは、ハードコーディングされたトリガールール (例えば「スクリーンショットが利用可能 → @vision-translator」) を、エージェントが宣言した自己文書化された前提条件に置き換える。

### パイプラインステージの可視化

すべてのルーティング決定はステージ識別子を出力し、ユーザーが内部のステップ番号を学ぶことなくパイプラインの進行状況を追跡できるようにする:

```
[Stage: Tool Layer] → [Stage: Opinion Layer] → [Stage: Fusion Layer] → [Stage: Implementation Layer]
```

ステージとフェーズのマッピング:
- `Tool Layer` — 材料収集フェーズ
- `Opinion Layer` — 並列計画設計フェーズ (中級 / 旗舰 / フロントエンド)
- `Fusion Layer` — 計画のフュージョンと検証フェーズ
- `Implementation Layer` — コード実装と受け入れフェーズ

### 統一された進捗報告

成功パスと失敗パスは同じ報告フォーマットに従い、内部エージェント名を決して公開しない:

```
[Pipeline] mode=<lite|balanced|strict>  stage=<Tool Layer|Opinion Layer|Fusion Layer|Implementation Layer>  status=<idle|in_progress|complete|degraded|stuck>
  reason: <why this stage>
  path: <Tool Layer|Mid-tier chain|Flagship chain|Frontend chain>
  fallback: <recovery strategy>
```

ステータスインジケーター:
- `in_progress` — 現在のステージを実行中
- `complete` — ステージが正常に終了
- `degraded` — 部分的な入力で実行中、信頼度が低下
- `stuck` — すべての回復パスが尽き、ユーザーの介入が必要

### スウィフトパラレルショートカット

メインパイプラインが実行中のとき、スウィフトは独立した単純なサブタスクのために並行して派遣されることができる:

```
Main pipeline: Tool Layer → Opinion Layer → Fusion Layer → Implementation Layer
Parallel lane: swift (always ready, runs alongside main pipeline)
```

トリガー条件 (いずれか一つ):
- ユーザーの指示が明示的に並行作業を要求する ("Xを同時に行う", "Yもすぐに確認する")
- メインパイプラインの実行中に単純なサブタスクが発生する (例: アーキテクチャ計画が設計されている間にTODOを検索する)
- ユーザーが直接 @swift を呼び出す

スコープ制限:
- ✅ メインパイプラインの出力に依存しない独立したタスク
- ✅ 単純な操作: ファイル検索、grep、設定クエリ、フォーマット
- ❌ メインパイプラインの入力を生成するタスク
- ❌ オピニオンフュージョンタスク (直列でなければならない)
- ❌ 実装およびQAタスク (直列でなければならない)

スウィフトがメインパイプラインの前に終了した場合、結果は保持され、最後に一緒に返される。メインパイプラインが先に終了した場合、スウィフトの結果は即座に返される。スウィフトの失敗はメインパイプラインの実行に影響を与えない。

### MCP権限の分離

オピニオンレイヤーのエージェントは、コードを直接読み取ることを禁止されている (`read: deny` + `bash: deny`)、これによりツールレイヤーをバイパスして自分で材料を取得することを防ぐ:

- ツールレイヤー: コードを読み取り、ファイルを検索できる ( `read` / `bash` アクセスあり)
- オピニオンレイヤー: `read: deny` + `bash: deny`、ツールレイヤーからの材料に基づいてのみ計画できる
- フュージョンレイヤー: 同じ制限、3つの意見に基づいてのみフュージョンできる

> 注: このプロジェクトは、MCPサーバーを構成していません。「MCP権限の分離」という用語は、エージェントレベルのツール制限 (`read: deny` / `bash: deny`) を指し、MCPサーバーレベルの分離ではありません。

### タスクネスティング防御

すべての非ルーティングエージェントは `task: deny` を宣言し、子エージェントが再び task() を呼び出すのを防ぎ、再帰的なネスティングをブロックする:

- **レイヤー 1 (エージェントフロントマター)**: 各エージェントファイルは `task: deny` を宣言
- **レイヤー 2 (opencode.json)**: `permission.task` はコンシェルジュがエージェントを呼び出すことのみを許可; 非ルーティングエージェントはワーカーを呼び出すことをグローバルに拒否される
- **レイヤー 3 (プロンプトガード)**: コンシェルジュプロンプトは、サブエージェントを介して新しいパイプラインを起動することを禁じる制約で終了する

> 2026-07に追加。コンシェルジュ→ツールハンドラー→ツールハンドラーの三重ネスティングを発見した後。三層の冗長性は、1つのレイヤーが失敗してもブロックを保証する。

### 無材料フォールバック

オピニオンレイヤーが呼び出されるが材料がない場合 (ツールレイヤーが完全に失敗した場合)、ユーザーに尋ねる:

- 「プランを直接提供する」を選択 → 要件の説明に基づく純粋な論理的推論 (コードを読むことはない)
- 「ツールレイヤーを待つ」を選択 → 出力 WAITING、ツールレイヤーが回復した後に再試行

### エラー分類

ツールレイヤーは、失敗時に明確なエラーカテゴリを出力し、盲目的に再試行するのではなく:

- `ERROR_PROVIDER` — サーバー 502/503/タイムアウト
- `ERROR_AUTH` — 認証失敗
- `ERROR_UNKNOWN` — その他のエラー

---

## コスト

### 約90%の節約理由

MoAはコールボリューム加重ミックスで請求します：ツールレイヤーの約80%、ミッドティアの約18%、フラグシップの約2%。このセクションのコストテーブルの単価を使って、実効出力単価を推定します：

> **重要**：80/18/2の比率は**アーキテクチャによって設計された予想コールボリューム分布**であり、測定されたコスト比率ではありません。実際の使用はタスクの種類と複雑さに依存します。

| レイヤー      | シェア | 出力単価 /1M                                                                            | 加重     |
| ---------- | ----- | ------------------------------------------------------------------------------------------------ | -------- |
| ツールレイヤー | 80%   | $0.28                                                                                            | $0.224   |
| ミッドティア   | 18%   | ~$2.10 (MiniMax $1.20 / DeepSeek Pro $3.48 / Qwen Plus $1.60 / **Kimi K2.7 $4.00 mid-fuse** 平均) | $0.378   |
| フラグシップ   | 2%    | ~$6.00 (Qwen/GLM/MiniMax ~$4-7 + **Kimi K3 $15.00 flag-fuse**)                                   | $0.12    |

ブレンドされた実効出力単価 ≈ **$0.72 / 1M**。 "全フラグシップ GLM $7.50" と比較すると約10% → **約90%の節約**; "全ミッドティア DeepSeek Pro $3.48" と比較すると約21% → **約79%の節約**。 "90%の節約"の主張はフラグシップベースラインに対する実際の価値です。

### OpenCode Goプラン

MoAは[OpenCode Go](https://opencode.ai/docs/zh-cn/go/)プランに基づいています、**最初の月は$5、その後は$10/月**。

**使用制限:**

| 時間ウィンドウ   | クォータ |
| ------------- | ----- |
| 5時間ごと      | $12   |
| 週間          | $30   |
| 月間          | $60   |

制限はドルの価値によって定義されています。安価なモデル（Flash）はより頻繁に使用でき、高価なモデル（GLM）はあまり使用できません。

### レイヤーごとの月間クォータ

| レイヤー      | モデル           | 単価 (in/out per 1M) | 月間クォータ | コール頻度      |
| ---------- | --------------- | -------------------------- | ------------- | ------------------- |
| ツールレイヤー | Flash           | $0.14 / $0.28              | 158,150       | ~80%                |
| ツールレイヤー | MiMo-V2.5       | $0.14 / $0.28              | 150,400       | (自由に使用)        |
| オピニオン    | MiniMax M3      | $0.30 / $1.20              | 16,000        | ~18%                |
| オピニオン    | DeepSeek V4 Pro | $1.74 / $3.48              | 17,150        |                     |
| オピニオン    | Qwen3.7 Plus    | $0.40 / $1.60              | 21,600        |                     |
| フュージョン     | Kimi K2.7 Code  | $0.95 / $4.00              | 9,250         | ~2% (ミッドティアフューズ) |
| フュージョン     | Kimi K3         | $3.00 / $15.00             | 280           | ~2% (フラグシップフューズ) |
| フュージョン     | GLM-5.2         | $1.40 / $4.40              | 4,300         | ~2% (フロントエンドリード) |

> すべてのモデルIDは宣言のみです。お好みのモデルに置き換えてください。

![OpenCode Go 5時間ごとのクォータ](.github/quota-chart-en.svg)

### 制限に達した後

- **無料モデルのフォールバック** — Goが制限に達した後、無料モデルを使い続けることができます。
- **Zenバランスのフォールバック** — コンソールで「バランスを使用」を有効にします。Goの制限後、自動的にZenバランスを使用します。

### 無料モデル

OpenCode Zenは最後の手段として無料モデルを提供します：

| モデル                  | 特徴                           |
| ---------------------- | ------------------------------- |
| DeepSeek V4 Flash Free | 速いが、コンテキストが制限される       |
| MiMo-V2.5 Free         | より良い品質だが、遅くなる可能性がある |
| North Mini Code Free   | Cohereによって提供される              |
| Nemotron 3 Ultra Free  | NVIDIAの無料エンドポイント            |

> ⚠️ 無料モデルの制限：小さいコンテキストウィンドウ、応答が遅くなる可能性、データはトレーニングに使用される場合があります、限られた時間無料です。

---

## セキュリティ

| 保護                     | 効果                                                                                                                                                                                        |
| ------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| グローバルキャッチオール   | 未宣言のツールコール → ポップアップ確認                                                                                                                                                          |
| エージェント権限の分離     | 各エージェントは許可されたツールのみを使用できます                                                                                                                                                         |
| MCP権限の分離           | オピニオンレイヤーはコードの読み取りを禁止されます（読み取り：拒否 / bash：拒否）、ツールレイヤーのバイパスを防ぎます（プロジェクトにはMCPサーバーが構成されていません。「MCP」はエージェントレベルのツール制限を指します） |
| タスク3レイヤー防御        | 非ルーティングエージェントはタスクを拒否 → コンシェルジュホワイトリスト → プロンプトガード、再帰的ネストを防ぎます |
| フォールバックチェーン     | ツールレイヤーが失敗 → ユーザーに確認 → 待機/スキップ/無料モデル                                                                                                                                            |
| ワンクリックロールバック   | `.opencode/`を削除して復元します                                                                                                                                                                |

---

## ローカルモデル

Ollama / LM Studioのようなローカルモデルを混ぜることをサポートします：

```yaml
# .opencode/agents/mid-coder.md
model: ollama-local/qwen3-coder
```

[`docs/opencode-moa.md`](docs/opencode-moa.md)の付録Aを参照してください。

---

## 検証

リポジトリには`.opencode/tests/`の下に3つのチェックスクリプトが含まれています。レイヤー0は完全自動です。レイヤー1–2はOpenCode内で進むガイド付きチェックリストです。

```bash
# レイヤー0 — 静的チェック（自動、0トークン）
pwsh .opencode/tests/T0-static-verify.ps1
# 期待される結果：すべてPASS / FAIL=0（システムレベルのキーを使用して、WARNもPASSとしてカウント）

# 3つのレイヤーを一度に実行
pwsh .opencode/tests/run-all.ps1
```

| スクリプト                    | レイヤー | 何をするか                                                                            | モード                 |
| ------------------------- | ----- | --------------------------------------------------------------------------------------- | -------------------- |
| `T0-static-verify.ps1`    | 0     | ファイル構造、エージェント/コマンド/スキルのカウント、READMEアンカー、キーパスの正確性をチェック | 自動                 |
| `T1-behavioral-guide.ps1` | 1     | ルーティング / オピニオン / フュージョンの動作に関するステップバイステップのチェックリストを印刷 | 手動（OpenCode内） |
| `T2-moa-smoke-guide.ps1`  | 2     | `/moa-*`コマンドのエンドツーエンドのスモークテストチェックリストを印刷             | 手動（OpenCode内） |
| `run-all.ps1`             | 0–2   | T0を実行し、その後T1/T2のガイド付きチェックリストを印刷                           | 混合                |

---

## FAQ

### インストール

**Q: 既に opencode.json がある場合、上書きされますか？**
A: いいえ。インストールスクリプトは MoA の `permission`、`agent`、`default_agent` 設定のみをマージし、既存の `provider`、`model` などは保持します。元のファイルは自動的に `.bak.timestamp` としてバックアップされます。

**Q: Windows には `cp` コマンドがありません、どうすればいいですか？**
A: `Copy-Item` または `xcopy` を使用してください:

```powershell
# PowerShell
Copy-Item -Recurse -Force opencode-moa\.opencode .\.opencode
# CMD
xcopy opencode-moa\.opencode .\.opencode /E /I /Y
```

**Q: pwsh/jq なしでインストールできますか？**
A: はい。方法 1 (AI 自動デプロイ) または方法 3 (手動設定マージ) を使用してください。

**Q: デスクトップアプリでのインストール方法は？**
A: 方法 1 が最も便利です — `docs/opencode-moa.en.md` をチャットボックスにドラッグして AI に自動デプロイさせてください。方法 2/3 は最初にターミナル (CMD/PowerShell/Terminal) で操作する必要があります。

### 使用法

**Q: "concierge-router" が見えません？**
A: "30秒デプロイ → デプロイが成功したかどうかを確認する方法" の下にある3つのチェックを確認してください: プロジェクトルートの `opencode.json`、`.opencode/agents/` 下の22の .md、再起動後に `Tab` で切り替え (Windows デスクトップクライアント: `Ctrl+.` も機能します)。

**Q: `@tool-handler` からの応答がありません？**
A: `.opencode/agents/tool-handler.md` が存在し、フロントマターの形式が正しいことを確認してください。

**Q: "model not found" エラー？**
A: モデル ID の形式は `provider/model-id` である必要があります (例: `opencode-go/kimi-k2.7-code`)。対応するプロバイダーを設定ファイルに登録してください (システムレベルの `~/.config/opencode/opencode.json` またはプロジェクトの `opencode.json`)、その後 TUI 内で `/models` を使用して利用可能なモデルを確認してください。

**Q: 元のビルド/プランエージェントに戻すにはどうすればいいですか？**
A: `Tab` を押して切り替えます (Windows デスクトップクライアント: `Ctrl+.` も機能します)、または `/build`、`/plan` と入力します。MoA は組み込みエージェントに影響を与えません。

**Q: Go プランではなく、自分のモデルを使用したいですか？**
A: エージェントの `model` フィールドを変更するだけです:

```yaml
# .opencode/agents/mid-eng.md
model: opencode-go/glm-5.2
```

**Q: デプロイ後にリポジトリを削除できますか？**
A: はい。MoA は既にプロジェクトの `.opencode/` ディレクトリにコピーされていますので、元のリポジトリは削除できます。

**Q: 複数のプロジェクトにデプロイするにはどうすればいいですか？**
A: 各プロジェクトを個別にデプロイしてください。`.opencode/` はプロジェクトレベルの設定であり、他のプロジェクトには影響しません。

### フォールバック

**Q: ツールレイヤー全体がダウンしています、どうすればいいですか？**
A: 上記の "フォールトトレランス設計 → フォールバックチェーン" を参照してください: MoA はユーザーに A. 数分待つ / B. ツールレイヤーをスキップして意見レイヤーを直接呼び出す (コストが高くなる) を選択するように求めます。

**Q: 無料モデルはどこにありますか？**
A: 上記の "コスト → 無料モデル" を参照してください: `/models` を使用してモデルリストを開き、"Free" とタグ付けされたものを選択してください (Windows デスクトップクライアント: `Ctrl+'` も機能します) (DeepSeek V4 Flash Free、MiMo-V2.5 Free、North Mini Code Free など)。無料モデルはコンテキストが限られており、遅くなる可能性があり、データはトレーニングに使用される場合があります。

---


## メンテナーツール (エンドユーザーには不要)

以下のファイルは **リポジトリのメンテナ** のためのものであり、MoA のデプロイには使用しません。エンドユーザーは無視できます。

| ファイル                       | 目的                                                                                                                                               |
| -------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------- |
| `deploy-sync.ps1`          | メンテナのみ — リポジトリを GitHub に同期し、`opencode-moa` スキルを SkillHub にアップロードします。 `-SkipGit` / `-SkipSkillHub` / `-DryRun` をサポートします。   |
| `scripts/hooks/pre-commit` | ローカル git フックリマインダー: `CHANGELOG.md` の変更をステージすると警告します (これは `master` へのプッシュ時に自動リリースされます)。                                   |
| `scripts/hooks/pre-push`   | ローカル git フックリマインダー: `CHANGELOG.md` の変更を `master` にプッシュする前にバージョンを確認します; 非対話型/CI 環境では自動的に進行します。 |

> これらのフックは自動的にはインストールされません。リマインダーが必要な場合は、`.git/hooks/` にシンボリックリンクを作成してください。例: `ln -s ../../scripts/hooks/pre-push .git/hooks/pre-push`。

---


## 貢献

PR と Issues を歓迎します。 [CONTRIBUTING.md](CONTRIBUTING.md) を参照してください。

---


## ライセンス

[MIT](LICENSE) · [OpenCode MoA](https://github.com/ZenHG/opencode-moa)
